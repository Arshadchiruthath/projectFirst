<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Home</title>
    <link rel="stylesheet" href="/public/style/userhome.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin-top: 50px;
        }

        input {
            margin: 5px;
            padding: 5px;
        }

        button {
            background-color: #007BFF;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }

    </style>
</head>
<body>
    
    <p>You are now logged in.</p>
    <a href="/user/Logout">
        <button>Logout</button>
    </a>
   
</body>
</html> -->

<!-- views/dashboard.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            background-color: #000;
            color: #fff;
            min-height: 100vh;
        }
        
        .gradient-overlay {
            background: linear-gradient(135deg, rgba(137, 61, 255, 0.1), rgba(32, 89, 255, 0.1));
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            pointer-events: none;
        }
        
        .glass-card {
            background: rgba(33, 37, 41, 0.7);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
        }
        
        .gradient-text {
            background: linear-gradient(to right, #a855f7, #3b82f6);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }
        
        .nav-glass {
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .decoration-circle {
            position: fixed;
            width: 300px;
            height: 300px;
            border-radius: 50%;
            filter: blur(80px);
            z-index: -1;
        }
        
        .circle-1 {
            background: rgba(137, 61, 255, 0.15);
            top: 20%;
            left: 20%;
        }
        
        .circle-2 {
            background: rgba(32, 89, 255, 0.15);
            bottom: 20%;
            right: 20%;
        }
        
        .animated-dots {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
    </style>
</head>
<body>
    <!-- Gradient Overlay -->
    <div class="gradient-overlay"></div>
    
    <!-- Decorative Circles -->
    <div class="decoration-circle circle-1"></div>
    <div class="decoration-circle circle-2"></div>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg nav-glass mb-4">
        <div class="container">
            <span class="navbar-brand mb-0 h1 gradient-text fs-4">Dashboard</span>
            <form action="/user/logout" method="GET" class="ms-auto">
                <button type="submit" class="btn btn-outline-light">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-box-arrow-right me-2" viewBox="0 0 16 16">
                        <path fill-rule="evenodd" d="M10 12.5a.5.5 0 0 1-.5.5h-8a.5.5 0 0 1-.5-.5v-9a.5.5 0 0 1 .5-.5h8a.5.5 0 0 1 .5.5v2a.5.5 0 0 0 1 0v-2A1.5 1.5 0 0 0 9.5 2h-8A1.5 1.5 0 0 0 0 3.5v9A1.5 1.5 0 0 0 1.5 14h8a1.5 1.5 0 0 0 1.5-1.5v-2a.5.5 0 0 0-1 0v2z"/>
                        <path fill-rule="evenodd" d="M15.854 8.354a.5.5 0 0 0 0-.708l-3-3a.5.5 0 0 0-.708.708L14.293 7.5H5.5a.5.5 0 0 0 0 1h8.793l-2.147 2.146a.5.5 0 0 0 .708.708l3-3z"/>
                    </svg>
                    Logout
                </button>
            </form>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container py-4">
        <div class="glass-card p-5 text-center">
            <h1 class="display-4 gradient-text mb-4">Welcome <%= username %></h1>
            <p class="lead text-light opacity-75">Experience the future of digital interaction</p>
            
            <!-- Animated Dots -->
            <div class="d-flex justify-content-center gap-3 mt-4">
                <div class="animated-dots">
                    <div class="rounded-circle bg-primary" style="width: 12px; height: 12px;"></div>
                </div>
                <div class="animated-dots" style="animation-delay: 0.5s;">
                    <div class="rounded-circle bg-info" style="width: 12px; height: 12px;"></div>
                </div>
                <div class="animated-dots" style="animation-delay: 1s;">
                    <div class="rounded-circle bg-purple" style="width: 12px; height: 12px;"></div>
                </div>
